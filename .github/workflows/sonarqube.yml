name: SonarQube Cloud Scan

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube-scan:
    name: Static Code Analysis
    runs-on: ubuntu-latest
    steps:
      # 1. Download full repository history
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. Set up Flutter SDK
      - name: Set up Flutter
        uses: subosito/flutter-action@8a3c6b6e7c5c0b7d3d8a6e4d0e4c9c1e8f3c5a7b
        with:
          # Line 18 starts here; ensure it aligns with previous steps
          flutter-version: stable

      # 3. Install Flutter dependencies
      - name: Install Flutter Dependencies
        run: flutter pub get

      # 4. Run the SonarQube Cloud scan
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
